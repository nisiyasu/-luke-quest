# M02 — Three.js実行基盤

Status at registration: `NOT_STARTED`

## 原文
作業：実装時の公式資料でバージョン・CDN・モジュール読込を確認して固定。Canvas/WebGL描画、縦画面リサイズ、ロード表示、エラー表示、タッチ・キーボード、描画ループを実装。独立した状態を使う。
成果物：起動可能な検証ページと起動コマンド。
合格：対象ブラウザで表示・移動し、入力解除と再表示が動く。機能合格のみで美術合格とはしない。

## 前提工程
M00 DONE、M01の比較契約を適用可能な状態。

## 未達時の戻り先
M02のまま。ライブラリ/起動方針の前提が破綻した場合はM00、画面構図契約が原因ならM01へ戻す。

## Close gate
実ブラウザ/WebGLでの表示・移動・入力解除・再表示の証拠が揃うまで閉じない。

## 各回の作業記録
実施内容 / commit / 画面・動画証拠 / 検証結果 / 未完了 / 正確な次の一手 をコメントへ追記する。
