import * as THREE from 'three';

function rng(seed=1){let s=seed>>>0;return()=>((s=(s*1664525+1013904223)>>>0)/4294967296)}
function makeBankGrassMap(seed=417){
  const r=rng(seed),c=document.createElement('canvas');c.width=c.height=512;const x=c.getContext('2d');
  x.fillStyle='#587c3b';x.fillRect(0,0,512,512);
  for(let i=0;i<230;i++){const px=r()*512,py=r()*512,rad=12+r()*58,g=x.createRadialGradient(px,py,0,px,py,rad);g.addColorStop(0,i%5===0?'rgba(212,199,106,.20)':i%4===0?'rgba(31,69,32,.30)':i%3===0?'rgba(103,145,56,.28)':'rgba(83,121,48,.24)');g.addColorStop(1,'rgba(0,0,0,0)');x.fillStyle=g;x.beginPath();x.arc(px,py,rad,0,Math.PI*2);x.fill()}
  for(let i=0;i<7600;i++){const px=r()*512,py=r()*512,l=1+r()*4.5;x.strokeStyle=i%9===0?'rgba(223,213,132,.34)':i%4===0?'rgba(35,72,31,.42)':'rgba(113,153,64,.38)';x.lineWidth=.45+r()*1.05;x.beginPath();x.moveTo(px,py);x.lineTo(px+(r()-.5)*3.1,py-l);x.stroke()}
  for(let i=0;i<420;i++){x.fillStyle=i%7===0?'rgba(238,225,176,.55)':i%4===0?'rgba(82,58,36,.28)':'rgba(176,194,102,.26)';x.beginPath();x.arc(r()*512,r()*512,.7+r()*2.0,0,Math.PI*2);x.fill()}
  for(let i=0;i<260;i++){const px=r()*512,py=r()*512,rad=1.2+r()*2.2;x.fillStyle=i%5===0?'rgba(225,220,169,.40)':'rgba(42,92,39,.42)';for(let p=0;p<3;p++){const a=p*Math.PI*2/3+r()*.25;x.beginPath();x.ellipse(px+Math.cos(a)*rad,py+Math.sin(a)*rad,rad*1.15,rad*.68,a,0,Math.PI*2);x.fill()}}
  for(let i=0;i<34;i++){const x0=r()*512,y0=r()*512,len=55+r()*155,ang=r()*Math.PI*2;x.strokeStyle=i%3===0?'rgba(91,68,43,.22)':i%3===1?'rgba(32,65,31,.20)':'rgba(196,181,112,.13)';x.lineWidth=6+r()*18;x.lineCap='round';x.beginPath();x.moveTo(x0,y0);x.bezierCurveTo(x0+Math.cos(ang+.45)*len*.33,y0+Math.sin(ang+.45)*len*.33,x0+Math.cos(ang-.35)*len*.68,y0+Math.sin(ang-.35)*len*.68,x0+Math.cos(ang)*len,y0+Math.sin(ang)*len);x.stroke()}
  const t=new THREE.CanvasTexture(c);t.colorSpace=THREE.SRGBColorSpace;t.wrapS=t.wrapT=THREE.RepeatWrapping;t.repeat.set(3.65,3.10);t.anisotropy=8;return t;
}
function makeCliffMap(seed=811){const r=rng(seed),c=document.createElement('canvas');c.width=c.height=512;const x=c.getContext('2d');x.fillStyle='#777467';x.fillRect(0,0,512,512);for(let i=0;i<95;i++){const y=r()*512;x.strokeStyle=i%4===0?'rgba(47,49,43,.30)':i%3===0?'rgba(155,143,112,.20)':'rgba(86,78,62,.22)';x.lineWidth=2+r()*10;x.beginPath();x.moveTo(-20,y);for(let px=-20;px<540;px+=24)x.lineTo(px,y+Math.sin(px*.027+r()*6)*9+(r()-.5)*7);x.stroke()}for(let i=0;i<240;i++){x.fillStyle=i%5===0?'rgba(72,94,58,.24)':'rgba(55,48,39,.16)';x.beginPath();x.ellipse(r()*512,r()*512,4+r()*30,2+r()*9,r()*.6,0,Math.PI*2);x.fill()}const t=new THREE.CanvasTexture(c);t.colorSpace=THREE.SRGBColorSpace;t.wrapS=t.wrapT=THREE.RepeatWrapping;t.repeat.set(2.8,2.1);t.anisotropy=8;return t}
function deform(g,r,a=.22){const p=g.attributes.position;for(let i=0;i<p.count;i++){const x=p.getX(i),y=p.getY(i),z=p.getZ(i);p.setXYZ(i,x*(1+(r()-.5)*a),y*(1+(r()-.5)*a*.72),z*(1+(r()-.5)*a))}p.needsUpdate=true;g.computeVertexNormals();return g}
function makeFacetedRockGeometry(r,radius,variant=0){let geo;if(variant%3===0)geo=new THREE.DodecahedronGeometry(radius,0);else if(variant%3===1)geo=new THREE.OctahedronGeometry(radius,0);else geo=new THREE.TetrahedronGeometry(radius*1.08,0);const p=geo.attributes.position;for(let i=0;i<p.count;i++){let x=p.getX(i),y=p.getY(i),z=p.getZ(i);if(variant%3===0){x*=1.22;y*=.66;z*=1.02}else if(variant%3===1){x*=1.06+(y>0?.16:-.04);y*=1.02;z*=.82}else{x*=.76;y*=1.34;z*=.80}const j=1+(r()-.5)*.20;p.setXYZ(i,x*j,y*(1+(r()-.5)*.12),z*(1+(r()-.5)*.18))}p.needsUpdate=true;geo.computeVertexNormals();return geo}
function makeFracturedCliffShelf(r,width,depth,facing,level=0){const seg=Math.max(18,Math.ceil(width*2.4)),verts=[],idx=[],uv=[];for(let i=0;i<=seg;i++){const t=i/seg,x=-width*.5+t*width,wave=Math.sin(t*Math.PI*(2.15+level*.31)+level*.73)*(.34-level*.035)+Math.sin(t*Math.PI*(5.7+level*.41)+1.17)*.12+(r()-.5)*.10,front=facing*(depth*.5-.34-level*.23+wave),back=front-facing*(.72+level*.10+(r()-.5)*.10),top=-.13-level*.31+Math.sin(t*Math.PI*3.3+level)*.06+(r()-.5)*.035,bottom=top-(.46+level*.13+Math.sin(t*Math.PI*2.4+.8)*.07);verts.push(x,top,back,x,top+(r()-.5)*.04,front,x+(r()-.5)*.09,bottom,front-facing*(.08+r()*.12));uv.push(t,0,t,.22,t,1)}for(let i=0;i<seg;i++){const a=i*3,b=a+3;idx.push(a,b,a+1,b,b+1,a+1,a+1,b+1,a+2,b+1,b+2,a+2)}const geo=new THREE.BufferGeometry();geo.setAttribute('position',new THREE.Float32BufferAttribute(verts,3));geo.setAttribute('uv',new THREE.Float32BufferAttribute(uv,2));geo.setIndex(idx);geo.computeVertexNormals();const m=new THREE.Mesh(geo,rockMats[(level*2+1)%rockMats.length]);m.castShadow=true;m.receiveShadow=true;return m}
function shadows(root){root.traverse(n=>{if(n.isMesh){n.castShadow=true;n.receiveShadow=true}});return root}
const grass=new THREE.MeshStandardMaterial({color:0xf8ffe9,map:makeBankGrassMap(417),roughness:.97,side:THREE.DoubleSide}),soil=new THREE.MeshStandardMaterial({color:0x514332,roughness:1}),soilLight=new THREE.MeshStandardMaterial({color:0x70563a,roughness:1,side:THREE.DoubleSide}),moss=new THREE.MeshStandardMaterial({color:0x587941,roughness:1}),mossLight=new THREE.MeshStandardMaterial({color:0x719552,roughness:1}),rootMat=new THREE.MeshStandardMaterial({color:0x4b3427,roughness:1}),cliffMap=makeCliffMap();
const rockMats=[0x5d6159,0x777266,0x898171,0x62675f,0x75786a,0x555d56].map((c,i)=>new THREE.MeshStandardMaterial({color:c,map:i%2?cliffMap:null,roughness:.96,metalness:0,flatShading:true})),fernMats=[0x2b5a2b,0x689445,0x487838].map(c=>new THREE.MeshStandardMaterial({color:c,roughness:.98,side:THREE.DoubleSide})),topGrassMats=[0x315c2d,0x477b38,0x71954b].map(c=>new THREE.MeshStandardMaterial({color:c,roughness:1})),flowerMats=[0xf4ead5,0xdcc8f2,0xf1df83].map(c=>new THREE.MeshStandardMaterial({color:c,roughness:.92,side:THREE.DoubleSide}));
function makeFernPatch(r,scale=.7,count=14){const g=new THREE.Group(),verts=[],idx=[],addTri=(a,b,c)=>{const n=verts.length/3;verts.push(...a,...b,...c);idx.push(n,n+1,n+2)},stemW=.028;verts.push(-stemW,0,0,stemW,0,0,stemW,0,1.05,-stemW,0,1.05);idx.push(0,1,2,0,2,3);for(let j=0;j<6;j++){const z=.16+j*.14,span=.22*(1-j*.07),tip=z+.17;addTri([-stemW,0,z],[-span,0,z+.055],[0,0,tip]);addTri([stemW,0,z],[span,0,z+.055],[0,0,tip])}const geo=new THREE.BufferGeometry();geo.setAttribute('position',new THREE.Float32BufferAttribute(verts,3));geo.setIndex(idx);geo.computeVertexNormals();const groups=[[],[],[]],dummy=new THREE.Object3D();for(let i=0;i<count;i++){const a=r()*Math.PI*2,rad=Math.sqrt(r())*.76*scale,h=(.55+r()*.55)*scale,w=(.65+r()*.35)*scale;groups[i%3].push({x:Math.cos(a)*rad,z:Math.sin(a)*rad,a,h,w,lean:.42+r()*.30,twist:r()-.5})}fernMats.forEach((mat,mi)=>{const list=groups[mi],mesh=new THREE.InstancedMesh(geo,mat,list.length);list.forEach((v,i)=>{dummy.position.set(v.x,.02*scale,v.z);dummy.rotation.set(-v.lean,-v.a+Math.PI/2,v.twist);dummy.scale.set(v.w,1,v.h);dummy.updateMatrix();mesh.setMatrixAt(i,dummy.matrix)});mesh.instanceMatrix.needsUpdate=true;mesh.castShadow=false;mesh.receiveShadow=true;g.add(mesh)});return g}
function makeOrganicTop(r,seed,width,depth,facing){const cols=32,rows=9,vertices=[],indices=[],uvs=[];for(let i=0;i<=cols;i++){const tx=i/cols,baseX=-width*.5+tx*width,frontWave=Math.sin(tx*Math.PI*3+seed*.19)*.42+Math.sin(tx*Math.PI*7+seed*.07)*.14+(r()-.5)*.08,backWave=Math.sin(tx*Math.PI*2.35+seed*.23)*.23+Math.sin(tx*Math.PI*5.2+seed*.09)*.09,front=facing*(depth*.5-.42+frontWave),back=-facing*(depth*.5-.18+backWave);for(let j=0;j<=rows;j++){const tz=j/rows,sideInfluence=Math.pow(Math.abs(tx-.5)*2,5.5),sideSign=tx<.5?-1:1,sideWave=Math.sin(tz*Math.PI*3.1+seed*.17+sideSign*.8)*.22+Math.sin(tz*Math.PI*6.3+seed*.07)*.07,x=baseX+sideSign*sideInfluence*sideWave,z=THREE.MathUtils.lerp(back,front,tz),centerLift=Math.sin(Math.PI*tz)*(.14+.065*Math.sin(tx*Math.PI*4+seed*.11)),crossRoll=Math.sin(tx*Math.PI*2.1+tz*Math.PI*1.7+seed*.13)*.075,macroRoll=Math.sin(tx*Math.PI*1.35+seed*.21)*Math.sin(tz*Math.PI)*.055,micro=(r()-.5)*.032,frontLift=tz*tz*(.045+.030*Math.sin(tx*Math.PI*2.2));vertices.push(x,.015+centerLift+crossRoll+macroRoll+micro+frontLift,z);uvs.push(tx,tz)}}const stride=rows+1;for(let i=0;i<cols;i++)for(let j=0;j<rows;j++){const a=i*stride+j,b=a+1,c=(i+1)*stride+j,d=c+1;indices.push(a,c,b,c,d,b)}const geo=new THREE.BufferGeometry();geo.setAttribute('position',new THREE.Float32BufferAttribute(vertices,3));geo.setAttribute('uv',new THREE.Float32BufferAttribute(uvs,2));geo.setIndex(indices);geo.computeVertexNormals();const top=new THREE.Mesh(geo,grass);top.receiveShadow=true;return top}
function addTopMicrodetail(g,r,width,depth,facing){const bladeGeo=new THREE.ConeGeometry(.038,.30,5,1,false),dummy=new THREE.Object3D(),lists=[[],[],[]],count=Math.max(150,Math.floor(width*16));for(let i=0;i<count;i++){const x=(r()-.5)*width*.88,z=(r()-.5)*depth*.64-facing*.28;if(r()<.10)continue;lists[i%3].push({x,z,h:.45+r()*.70,s:.55+r()*.65,ry:r()*Math.PI})}lists.forEach((list,mi)=>{const mesh=new THREE.InstancedMesh(bladeGeo,topGrassMats[mi],list.length);list.forEach((v,i)=>{dummy.position.set(v.x,.12+v.h*.09,v.z);dummy.rotation.set((i%3-1)*.10,v.ry,(i%5-2)*.04);dummy.scale.set(v.s,v.h,v.s);dummy.updateMatrix();mesh.setMatrixAt(i,dummy.matrix)});mesh.instanceMatrix.needsUpdate=true;mesh.castShadow=false;mesh.receiveShadow=true;g.add(mesh)});for(let i=0;i<22;i++){const radius=.15+r()*.36,patch=new THREE.Mesh(new THREE.CircleGeometry(radius,10),i%3===0?soilLight:soil);patch.rotation.x=-Math.PI/2;patch.position.set((r()-.5)*width*.80,.105+(r()-.5)*.02,(r()-.5)*depth*.52-facing*.38);patch.scale.set(1.5,.65,1);g.add(patch)}const petalGeo=new THREE.CircleGeometry(.045,7),stemGeo=new THREE.CylinderGeometry(.008,.012,.16,5),flowerGroups=[[],[],[]],islandCount=Math.max(10,Math.floor(width*1.35));for(let k=0;k<islandCount;k++){const cx=(r()-.5)*width*.76,cz=(r()-.5)*depth*.50-facing*.32,n=2+Math.floor(r()*5);for(let j=0;j<n;j++)flowerGroups[(k+j)%3].push({x:cx+(r()-.5)*.42,z:cz+(r()-.5)*.42,s:.70+r()*.75,ry:r()*Math.PI})}flowerGroups.forEach((list,mi)=>{const petals=new THREE.InstancedMesh(petalGeo,flowerMats[mi],list.length),stems=new THREE.InstancedMesh(stemGeo,fernMats[0],list.length);list.forEach((v,i)=>{dummy.position.set(v.x,.24,v.z);dummy.rotation.set(-Math.PI/2,0,v.ry);dummy.scale.set(v.s,v.s,v.s);dummy.updateMatrix();petals.setMatrixAt(i,dummy.matrix);dummy.position.set(v.x,.16,v.z);dummy.rotation.set(0,0,0);dummy.scale.set(.7,.9,.7);dummy.updateMatrix();stems.setMatrixAt(i,dummy.matrix)});petals.instanceMatrix.needsUpdate=true;stems.instanceMatrix.needsUpdate=true;petals.castShadow=false;stems.castShadow=false;g.add(stems,petals)})}
export function createTargetBankV4({seed=1,width=12,depth=8,facing=1}={}){const requestedDepth=depth,integration509=seed===509;if(integration509)depth=10;const r=rng(seed),g=new THREE.Group();g.name=`bank_target_v4_${seed}`;g.add(makeOrganicTop(r,seed,width,depth,facing));addTopMicrodetail(g,r,width,depth,facing);
  for(let level=0;level<4;level++)g.add(makeFracturedCliffShelf(r,width,depth,facing,level));
  const bands=[{z:depth*.5-.28,y:-.31,scale:.78},{z:depth*.5-.58,y:-.72,scale:.60},{z:depth*.5-.84,y:-1.08,scale:.43}];
  bands.forEach((band,bi)=>{const count=Math.max(4,Math.ceil(width/(bi===0?1.75:2.15)));for(let i=0;i<count;i++){if((i*3+bi+seed)%5!==0)continue;const t=(i+.5)/count,x=-width*.5+t*width+(r()-.5)*.48,edgeWave=Math.sin(t*Math.PI*2.7+seed*.19)*.46+Math.sin(t*Math.PI*6.2+seed*.07)*.17,radius=(.43+r()*.42)*band.scale,variant=(i+bi+seed)%3,geo=makeFacetedRockGeometry(r,radius,variant),rock=new THREE.Mesh(geo,rockMats[(i*2+bi+seed)%rockMats.length]);rock.scale.set(.92+r()*.72,.72+r()*.52,.76+r()*.56);rock.position.set(x,band.y+(r()-.5)*.22,facing*(band.z+edgeWave)+(r()-.5)*.20);rock.rotation.set((r()-.5)*.48,r()*Math.PI,(r()-.5)*.38);g.add(rock);if(bi===0&&i%3===0){const cap=new THREE.Mesh(new THREE.CircleGeometry(radius*(.58+r()*.18),7),i%2?moss:mossLight);cap.rotation.x=-Math.PI/2;cap.position.set(rock.position.x,rock.position.y+radius*.35,rock.position.z-facing*.04);cap.rotation.z=r()*Math.PI;g.add(cap)}}});
  const ledgeMat=[soilLight,rockMats[1],rockMats[4],moss],ledgeCount=Math.max(6,Math.ceil(width/1.9));for(let i=0;i<ledgeCount;i++){const t=(i+.35+r()*.18)/ledgeCount,edgeWave=Math.sin(t*Math.PI*2.7+seed*.19)*.46+Math.sin(t*Math.PI*6.2+seed*.07)*.17,w=.75+r()*1.55,h=.10+r()*.20,d=.28+r()*.54,ledge=new THREE.Mesh(deform(new THREE.BoxGeometry(w,h,d,4,1,2),r,.34),ledgeMat[(i+seed)%ledgeMat.length]);ledge.position.set(-width*.5+t*width+(r()-.5)*.55,-.46-r()*1.02,facing*(depth*.5-.60+edgeWave)+(r()-.5)*.28);ledge.rotation.set((r()-.5)*.14,(r()-.5)*.42,(r()-.5)*.13);g.add(ledge)}
  const clusters=Math.max(9,Math.ceil(width/1.08));for(let i=0;i<clusters;i++){if((i+seed)%6===4)continue;const t=(i+r()*.35)/Math.max(1,clusters-1),edgeWave=Math.sin(t*Math.PI*2.7+seed*.19)*.46+Math.sin(t*Math.PI*6.2+seed*.07)*.17,fern=makeFernPatch(r,.48+r()*.48,12+(i%4)*5);fern.position.set(-width*.45+t*width*.90+(r()-.5)*.34,.10+(r()-.5)*.07,facing*(depth*.5-.58+edgeWave));fern.rotation.y=r()*Math.PI*2;g.add(fern)}
  for(let i=0;i<12;i++){const rad=.18+r()*.30,stone=new THREE.Mesh(makeFacetedRockGeometry(r,rad,(i+seed)%3),rockMats[(i+seed)%rockMats.length]);stone.scale.set(.82+r()*.72,.52+r()*.55,.72+r()*.62);stone.position.set((r()-.5)*width*.84,.08+(r()-.5)*.08,(r()-.5)*depth*.52-facing*.66);stone.rotation.set((r()-.5)*.42,r()*Math.PI,(r()-.5)*.35);g.add(stone)}
  const toeCount=Math.max(9,Math.ceil(width/.96));for(let i=0;i<toeCount;i++){if((i+seed)%6===2)continue;const t=(i+.25+r()*.22)/toeCount,edgeWave=Math.sin(t*Math.PI*2.7+seed*.19)*.46+Math.sin(t*Math.PI*6.2+seed*.07)*.17,rad=.15+r()*.25,toe=new THREE.Mesh(makeFacetedRockGeometry(r,rad,(i+seed+3)%3),rockMats[(i+seed+3)%rockMats.length]);toe.scale.set(.90+r()*.80,.45+r()*.48,.72+r()*.62);toe.position.set(-width*.5+t*width+(r()-.5)*.34,-1.45+r()*.24,facing*(depth*.5-1.04+edgeWave)+(r()-.5)*.26);toe.rotation.set((r()-.5)*.34,r()*Math.PI,(r()-.5)*.28);g.add(toe)}
  const rootCount=Math.max(5,Math.floor(width/2.1));for(let i=0;i<rootCount;i++){const t=(i+.55)/rootCount,edgeWave=Math.sin(t*Math.PI*3+seed*.19)*.42,len=.65+r()*.75,root=new THREE.Mesh(new THREE.CylinderGeometry(.028+r()*.025,.055+r()*.035,len,6),rootMat);root.position.set(-width*.46+t*width*.92,-.34-len*.34,facing*(depth*.5-.42+edgeWave));root.rotation.z=(r()-.5)*.28;root.rotation.x=facing*(.10+r()*.18);g.add(root)}
  if(integration509)for(const child of g.children){child.position.z-=.8;child.position.y-=.10}g.userData={assetId:'bank_target_v4',version:'5.0.0',seed,stage:'M04_QUALITY_REGATE',source:'repo-procedural',intent:'target-facing forest bank with sparse non-periodic fractured strata, larger silhouette variation, cliff texture breakup, broad erosion veins and integrated vegetation; v5.0 removes bead-row cadence while preserving accepted macro placement',integrationProfile:integration509?'m06-p01-bank509-mid':'default',requestedDepth,effectiveDepth:depth};return shadows(g)}